#!/bin/sh

ARGS=$@
if [ "$ARGS" ]
then
	for TARGET in $ARGS
	do
		echo $TARGET >> targets-exceptions.txt
	done
else
	cat >> targets-targets.txt
fi

TMPFILE=`mktemp`; cat targets-exceptions.txt | sort | uniq > $TMPFILE; mv $TMPFILE targets-exceptions.txt
